# Firmic B14 Sidebar OS

Add `src/os/sidebar/`. Replace `components/FirmicSidebar.tsx`, `components/AdminSidebar.tsx`, and `pages/_app.tsx`.

Features: grouped tenant/admin navigation, active-route detection, collapse mode, mobile drawer, favorites, workspace card, notification badge, and separate admin tools.

Restart with:

```bash
rm -rf .next
npm run dev
```
