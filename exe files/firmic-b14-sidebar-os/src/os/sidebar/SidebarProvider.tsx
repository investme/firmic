import {type ReactNode,useCallback,useEffect,useMemo,useState} from "react";import {SidebarContext} from "./SidebarContext";
const COLLAPSED_KEY="firmic-sidebar-collapsed";const FAVORITES_KEY="firmic-sidebar-favorites";
function readFavorites(){if(typeof window==="undefined")return[];try{const v=JSON.parse(localStorage.getItem(FAVORITES_KEY)||"[]");return Array.isArray(v)?v.filter((x):x is string=>typeof x==="string"):[]}catch{return[]}}
export function SidebarProvider({children}:{children:ReactNode}){const[collapsed,setCollapsed]=useState(false);const[mobileOpen,setMobileOpen]=useState(false);const[favorites,setFavorites]=useState<string[]>([]);useEffect(()=>{setCollapsed(localStorage.getItem(COLLAPSED_KEY)==="true");setFavorites(readFavorites())},[]);const toggleCollapsed=useCallback(()=>setCollapsed(c=>{const n=!c;localStorage.setItem(COLLAPSED_KEY,String(n));return n}),[]);const toggleFavorite=useCallback((id:string)=>setFavorites(c=>{const n=c.includes(id)?c.filter(x=>x!==id):[...c,id];localStorage.setItem(FAVORITES_KEY,JSON.stringify(n));return n}),[]);const value=useMemo(()=>({collapsed,mobileOpen,favorites,toggleCollapsed,openMobile:()=>setMobileOpen(true),closeMobile:()=>setMobileOpen(false),toggleFavorite}),[collapsed,mobileOpen,favorites,toggleCollapsed,toggleFavorite]);return <SidebarContext.Provider value={value}>{children}</SidebarContext.Provider>}
